# Lists all files and directories with additional information 
export alias ll = ls -la

# Public IP address
export alias myip = dig +short myip.opendns.com @resolver1.opendns.com

# Quit/exit current shell session
export alias quit = exit

# Reload the nushell
export alias reload = exec nu